import { Messages } from 'types/i18n'

const messages: Messages = {
    'countdown.complete': 'Refresh the page to load a new game!',
    'countdown.remainingTime': 'New game in {hours}:{minutes}:{seconds}',
}

export default messages
