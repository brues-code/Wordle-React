export enum LocalStorage {
    GAME_STATE = 'gamestate',
    LOCALE = 'locale',
}

export enum Locales {
    en = 'en',
}

export enum SpecialKeys {
    Backspace = 'Backspace',
    Enter = 'Enter',
}

export enum GuessType {
    Invalid,
    WrongIndex,
    Correct,
}
