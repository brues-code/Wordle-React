export enum Cookies {
    GUESSES = 'guesses',
    LOCALE = 'locale',
}

export enum Locales {
    en = 'en',
}

export enum KeyCode {
    Backspace = 'Backspace',
    Enter = 'Enter',
}

export enum GuessType {
    Invalid,
    WrongIndex,
    Correct,
}
