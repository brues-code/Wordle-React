export enum Cookies {
    GUESSES = 'guesses',
    LOCALE = 'locale',
}

export enum Locales {
    en = 'en',
}

export enum KeyCode {
    Backspace = 8,
    Enter = 13,
    KeyA = 65,
    KeyZ = 90,
}

export enum GuessType {
    Invalid,
    WrongIndex,
    Correct,
}
